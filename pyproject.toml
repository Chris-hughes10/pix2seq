[tool.black]
line-length = 100
include = '\.pyi?$'
exclude = '''
    /(
        \.git
      | _build
      | build
      | dist
    ) '''

[tool.isort]
profile = "black"

[tool.pytest.ini_options]
markers = [
    "slow: longer-running tests (e.g. the RL overfit test)",
]